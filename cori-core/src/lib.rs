/// Cori Core — the agent implementation lives here.
/// Each module corresponds to a course session.
///
/// Sessions are added incrementally as you work through the lessons.
/// Right now: empty. That's intentional — you'll fill this in.

pub mod lesson;

// Session 01
pub mod types;
pub mod loop_;
mod loop_test;

// Session 02
pub mod tools;

// Session 03
pub mod claude;

// Session 04
pub mod context;
